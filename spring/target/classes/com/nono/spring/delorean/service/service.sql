SELECT BTNCD, ACTNO, SEIKIN AS AMT
  FROM `test`.`tori`
  WHERE UKE_D = DATE('20121018')
  
  cross join
  
  select 
  
UNION ALL
SELECT BTNCD, ACTNO, SYOZEI AS AMT
  FROM `test`.`tori`
  WHERE UKE_D = DATE('20121018')
UNION ALL
SELECT BTNCD, ACTNO, JYUZEI AS AMT
  FROM `test`.`tori`
  WHERE UKE_D = DATE('20121018')
